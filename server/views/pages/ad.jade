extends ../layouts/layout

block content
  .row
    .col-md-6.col-md-offset-3
      form(role='form', action='/auth/ad', method='post')
        button.btn.btn-default(type='submit') Submit
      br
      if (messages.error)
        p
          span(style="color:red")= messages.error