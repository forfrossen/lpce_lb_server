module.exports = function(app) {
	var ACL = app.models.ACL;
	ACL.create({
	  model: 'project',
	  accessType: 'EXECUTE',
	  principalType: 'ROLE',
	  principalId: '$owner',
	  permission: 'ALLOW',
	  property: 'withdraw'
	}, function (err, acl) {
	  console.log('ACL entry created: %j', acl);
	});
  }