module.exports = function(F4801, options) {
	/*
	Object.defineProperty(Fremdarbeit.prototype, 
		'auftragline', {
			get: function(){
				return [this.auftrag, this.wlopsq].join('_').trim();
			}
		}
	)
	F4801.computeFremdarbeitID = function computeFremdarbeitID(f4801) {
  		//return 'Test';
  		return f4801.wadoco + '_' + f4801.wlopsq*100;
	};
	*/
} 	